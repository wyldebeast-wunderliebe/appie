#!/bin/sh

USAGE="zope:configure help | (<variable> <value> [append])"

if [ "$1" = "help" ]; then
    echo "$USAGE"
    exit 1
fi

VAR=$1
VAL=$2
APPEND=$3

if [ -z "$VAR" ]; then
    echo "Value cannot be empty"
    exit 1
fi


echo "Under construction!"

exit 666